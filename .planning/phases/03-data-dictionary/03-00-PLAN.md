---
phase: 03-data-dictionary
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/features/dictionary/actions/domain-actions.test.ts
  - src/features/dictionary/actions/field-actions.test.ts
  - src/features/dictionary/actions/value-actions.test.ts
  - src/features/dictionary/actions/version-actions.test.ts
  - src/features/dictionary/components/fields/field-data-table.test.tsx
  - src/features/dictionary/components/fields/field-form-panel.test.tsx
  - src/features/dictionary/components/domains/domain-data-table.test.tsx
  - src/features/dictionary/components/visualisation/dictionary-graph.test.tsx
  - src/features/dictionary/components/visualisation/tree-view.test.tsx
  - src/features/dictionary/hooks/use-graph-data.test.ts
  - src/features/dictionary/lib/validators.test.ts
  - src/features/dictionary/lib/csv-parser.test.ts
autonomous: true
requirements:
  - DD-01
  - DD-02
  - DD-03
  - DD-04
  - DD-05
  - DD-07
  - DD-08
  - DD-09
must_haves:
  truths:
    - "All dictionary test scaffold files exist and pass with placeholder assertions"
    - "pnpm vitest run completes with zero failures for dictionary test files"
  artifacts:
    - path: "src/features/dictionary/actions/domain-actions.test.ts"
      provides: "Domain CRUD server actions test scaffold"
    - path: "src/features/dictionary/actions/field-actions.test.ts"
      provides: "Field CRUD server actions test scaffold"
    - path: "src/features/dictionary/actions/value-actions.test.ts"
      provides: "Picklist/concatenated value actions test scaffold"
    - path: "src/features/dictionary/actions/version-actions.test.ts"
      provides: "Versioning actions test scaffold"
    - path: "src/features/dictionary/components/fields/field-data-table.test.tsx"
      provides: "Grouped field DataTable component test scaffold"
    - path: "src/features/dictionary/components/fields/field-form-panel.test.tsx"
      provides: "Conditional field form panel test scaffold"
    - path: "src/features/dictionary/components/domains/domain-data-table.test.tsx"
      provides: "Domain DataTable with drag-to-reorder test scaffold"
    - path: "src/features/dictionary/components/visualisation/dictionary-graph.test.tsx"
      provides: "Force graph component test scaffold"
    - path: "src/features/dictionary/components/visualisation/tree-view.test.tsx"
      provides: "Tree view component test scaffold"
    - path: "src/features/dictionary/hooks/use-graph-data.test.ts"
      provides: "Graph data transform hook test scaffold"
    - path: "src/features/dictionary/lib/validators.test.ts"
      provides: "Title case and field name validation test scaffold"
    - path: "src/features/dictionary/lib/csv-parser.test.ts"
      provides: "CSV parser test scaffold"
  key_links: []
---

<objective>
Create test scaffold files for all dictionary feature modules to satisfy Nyquist compliance before implementation begins.

Purpose: Ensure every module has a test file with placeholder assertions and commented test plans, following the Phase 2 test scaffold pattern established in 02-00.
Output: 12 test scaffold files in src/features/dictionary/ that all pass via `pnpm vitest run`.
</objective>

<execution_context>
@/Users/fabio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fabio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-dictionary/03-CONTEXT.md
@.planning/phases/03-data-dictionary/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server action and lib test scaffolds</name>
  <files>
    src/features/dictionary/actions/domain-actions.test.ts,
    src/features/dictionary/actions/field-actions.test.ts,
    src/features/dictionary/actions/value-actions.test.ts,
    src/features/dictionary/actions/version-actions.test.ts,
    src/features/dictionary/lib/validators.test.ts,
    src/features/dictionary/lib/csv-parser.test.ts
  </files>
  <read_first>
    src/features/ontology/actions/class-actions.test.ts,
    src/features/ontology/lib/validators.test.ts
  </read_first>
  <action>
Create 6 test scaffold files following the exact pattern from Phase 2 (describe block with passing placeholder + commented test plan).

**domain-actions.test.ts:**
```typescript
import { describe, it, expect } from "vitest";

describe("domain-actions", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - getDictionaryDomains returns domain list ordered by display_order
  // - createDictionaryDomain inserts domain with tenant_id, returns created domain
  // - createDictionaryDomain returns error on duplicate name (23505)
  // - updateDictionaryDomain updates name, description, domain_area, owner
  // - deleteDictionaryDomain removes domain and unlinks fields (no cascade)
  // - reorderDomains updates display_order for all provided domain IDs
});
```

**field-actions.test.ts:**
```typescript
import { describe, it, expect } from "vitest";

describe("field-actions", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - getDictionaryFields returns fields with domain assignments
  // - createDictionaryField inserts field with tenant_id
  // - createDictionaryField returns error on duplicate field_name (23505)
  // - updateDictionaryField updates all field properties
  // - deleteDictionaryField removes field and cascades to picklist_values, concatenated_refs
  // - assignFieldToDomains updates field-domain junction table
});
```

**value-actions.test.ts:**
```typescript
import { describe, it, expect } from "vitest";

describe("value-actions", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - getPicklistValues returns values for a field_id ordered by display_order
  // - savePicklistValues upserts values (insert new, remove deleted)
  // - getConcatenatedRefs returns referenced field_ids in position order
  // - saveConcatenatedRefs replaces all refs for a field
  // - uploadMatchTable parses CSV and stores in dictionary_match_tables
  // - getMatchTable returns match table data for a field
});
```

**version-actions.test.ts:**
```typescript
import { describe, it, expect } from "vitest";

describe("version-actions", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - getDictionaryVersions returns versions ordered by version_number desc
  // - publishDictionaryVersion creates snapshot with auto-incremented version_number
  // - publishDictionaryVersion stores correct JSONB snapshot structure
  // - getDictionaryVersionSnapshot returns full snapshot for a version
  // - computeVersionDiff returns added/removed/changed fields between two snapshots
});
```

**validators.test.ts:**
```typescript
import { describe, it, expect } from "vitest";

describe("validators", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - toTitleCase capitalises first letter of each word
  // - toTitleCase handles empty string
  // - toTitleCase handles single word
  // - toTitleCase handles already-Title-Case input
  // - toTitleCase handles mixed case input
  // - validateFieldName rejects empty strings
  // - validateFieldName rejects strings over 255 chars
});
```

**csv-parser.test.ts:**
```typescript
import { describe, it, expect } from "vitest";

describe("csv-parser", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - parseMatchTableCSV extracts columns from header row
  // - parseMatchTableCSV parses rows into Record<string, string>[]
  // - parseMatchTableCSV handles quoted fields with commas
  // - parseMatchTableCSV returns errors for malformed CSV
  // - parseMatchTableCSV skips empty lines
});
```

Create necessary directories: `src/features/dictionary/actions/`, `src/features/dictionary/lib/`.
  </action>
  <verify>
    <automated>cd /Users/fabio/AIWorkspace/DevProjects/ForgedcOrg/_internal/DataBridgeAI && pnpm vitest run src/features/dictionary/actions/ src/features/dictionary/lib/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/features/dictionary/actions/domain-actions.test.ts contains "describe(\"domain-actions\""
    - File src/features/dictionary/actions/field-actions.test.ts contains "describe(\"field-actions\""
    - File src/features/dictionary/actions/value-actions.test.ts contains "describe(\"value-actions\""
    - File src/features/dictionary/actions/version-actions.test.ts contains "describe(\"version-actions\""
    - File src/features/dictionary/lib/validators.test.ts contains "describe(\"validators\""
    - File src/features/dictionary/lib/csv-parser.test.ts contains "describe(\"csv-parser\""
    - All 6 files pass vitest with zero failures
  </acceptance_criteria>
  <done>6 server action and lib test scaffolds exist and pass with placeholder assertions</done>
</task>

<task type="auto">
  <name>Task 2: Create component and hook test scaffolds</name>
  <files>
    src/features/dictionary/components/fields/field-data-table.test.tsx,
    src/features/dictionary/components/fields/field-form-panel.test.tsx,
    src/features/dictionary/components/domains/domain-data-table.test.tsx,
    src/features/dictionary/components/visualisation/dictionary-graph.test.tsx,
    src/features/dictionary/components/visualisation/tree-view.test.tsx,
    src/features/dictionary/hooks/use-graph-data.test.ts
  </files>
  <read_first>
    src/features/ontology/components/class-list/class-data-table.test.tsx,
    src/features/ontology/components/graph/ontology-graph.test.tsx,
    src/features/ontology/hooks/use-ontology-sync.test.ts
  </read_first>
  <action>
Create 6 test scaffold files for components and hooks following Phase 2 pattern.

**field-data-table.test.tsx:**
```typescript
import { describe, it, expect } from "vitest";

describe("FieldDataTable", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - Renders domain group headers with colour dot and field count
  // - Renders fields within each domain group
  // - Fields in multiple domains appear under each domain group
  // - "Unassigned" group shown for fields without domain assignment
  // - Domain groups ordered by display_order
  // - Row click calls onRowClick with field object
  // - Collapse/expand toggles field visibility within group
});
```

**field-form-panel.test.tsx:**
```typescript
import { describe, it, expect } from "vitest";

describe("FieldFormPanel", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - Renders "Create Field" title when editingField is null
  // - Renders "Edit Field" title when editingField is provided
  // - Shows Manage Values button when Value Type = Picklist
  // - Shows Configure Fields button when Value Type = Concatenated
  // - Hides Manage Values / Configure Fields for other value types
  // - Shows Controlled? checkbox only when Value Type = Picklist AND matchTableExists
  // - Field Name auto-corrects to Title Case on blur
  // - Calls onSave with correct DictionaryFieldInput on submit
});
```

**domain-data-table.test.tsx:**
```typescript
import { describe, it, expect } from "vitest";

describe("DomainDataTable", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - Renders domains in display_order
  // - Shows colour dot, name, description, domain area, owner, field count
  // - Row click calls onRowClick with domain object
  // - Drag handle visible on each row
  // - Empty state shown when no domains
});
```

**dictionary-graph.test.tsx:**
```typescript
import { describe, it, expect } from "vitest";

describe("DictionaryGraph", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - Renders with dynamic import (SSR-safe)
  // - Renders loading skeleton while graph loads
  // - Passes graph data with nodes and links to ForceGraph2D
});
```

**tree-view.test.tsx:**
```typescript
import { describe, it, expect } from "vitest";

describe("TreeView", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - Renders domain nodes at root level
  // - Renders field nodes under their domain
  // - Renders "Unassigned" node for orphan fields
  // - Click domain toggles expand/collapse
  // - Click field calls onFieldClick
  // - Shows picklist values under expanded picklist fields
});
```

**use-graph-data.test.ts:**
```typescript
import { describe, it, expect } from "vitest";

describe("useGraphData", () => {
  it("scaffold — replace with real tests", () => {
    expect(true).toBe(true);
  });

  // Test plan:
  // - Transforms domains to domain nodes with colour from DOMAIN_COLOUR_PALETTE
  // - Transforms fields to field nodes with domain colour at 60% opacity
  // - Creates belongs-to links from fields to their domains
  // - Creates concatenated links between concatenated fields
  // - Unassigned fields get UNASSIGNED_COLOUR
  // - Domain nodes are larger (val: 20) than field nodes (val: 8)
});
```

Create necessary directories: `src/features/dictionary/components/fields/`, `src/features/dictionary/components/domains/`, `src/features/dictionary/components/visualisation/`, `src/features/dictionary/hooks/`.
  </action>
  <verify>
    <automated>cd /Users/fabio/AIWorkspace/DevProjects/ForgedcOrg/_internal/DataBridgeAI && pnpm vitest run src/features/dictionary/components/ src/features/dictionary/hooks/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/features/dictionary/components/fields/field-data-table.test.tsx contains "describe(\"FieldDataTable\""
    - File src/features/dictionary/components/fields/field-form-panel.test.tsx contains "describe(\"FieldFormPanel\""
    - File src/features/dictionary/components/domains/domain-data-table.test.tsx contains "describe(\"DomainDataTable\""
    - File src/features/dictionary/components/visualisation/dictionary-graph.test.tsx contains "describe(\"DictionaryGraph\""
    - File src/features/dictionary/components/visualisation/tree-view.test.tsx contains "describe(\"TreeView\""
    - File src/features/dictionary/hooks/use-graph-data.test.ts contains "describe(\"useGraphData\""
    - All 6 files pass vitest with zero failures
  </acceptance_criteria>
  <done>12 total dictionary test scaffolds exist and all pass with placeholder assertions</done>
</task>

</tasks>

<verification>
All 12 test scaffold files pass: `pnpm vitest run src/features/dictionary/ --reporter=verbose`
</verification>

<success_criteria>
- 12 test scaffold files created in src/features/dictionary/
- All pass vitest with zero failures
- Each file has a describe block with a passing placeholder and commented test plan
- Directory structure matches research recommendation
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-dictionary/03-00-SUMMARY.md`
</output>
